# CheryTools v2「Fable」项目交接说明

## 1. 项目背景

CheryTools 是一个面向《A Dance of Fire and Ice》的 Unity Mod Manager Mod。

经过长期迭代，CheryTools v1 已经覆盖了较多功能，包括：

- ImGui 控制面板；
- KeyViewer（KV）布局、按键显示与键雨；
- Overlayer（OV）文本、图片、Tag 与 Token 节点动画；
- 游戏 UI、输入与编辑器相关工具；
- 关卡列表等扩展功能；
- 多轮渲染、缓存、异步运行时和性能优化。

这些功能证明了 CheryTools 的产品方向，但 v1 是在长期需求增长中不断扩建形成的。功能、状态、渲染和配置之间已经存在较多历史耦合，继续在原有结构上叠加功能会显著提高维护风险。

因此，CheryTools v1 正式结束主动开发，最终封板版本为：

- **v1 源码目录：`E:\ADOFAI-MOD\CheryTools-main\CheryTools-main`**
- **版本：26.2 Beta**
- **安装包：`E:\ADOFAI-MOD\CheryTools-main\CheryTools-main\CheryTools_Beta_26.2.zip`**
- **封板前备份：`E:\ADOFAI-MOD\CheryTools-main\CheryTools-main\Backups\Before_FinalBetaTune_20260731_155230`**

除非发现影响基本使用的严重问题，否则不再对 v1 进行架构调整或盲目性能优化。

---

## 2. CheryTools v2：Fable

CheryTools v2 的开发代号为 **Fable**。

Fable 不是对 v1 代码进行一次大规模整理，也不是继续在 v1 上拆东墙补西墙，而是一次**彻底的、从基础架构开始的重构**。

v1 的定位是：

- 功能与交互参考；
- 已验证需求的样本；
- 游戏接口和逆向经验来源；
- 需要避免的技术债案例。

v1 **不应直接成为 v2 继续堆叠功能的底座**。可以参考、提取和重新实现其中经过验证的逻辑，但不能为了快速看到效果而整块复制旧架构。

---

## 3. Fable 的核心目标

1. **重新划分职责**  
   配置、游戏数据采集、业务运行时、渲染、编辑器 UI 和持久化应有清晰边界。

2. **建立统一生命周期**  
   明确 Mod 加载、启用、禁用、场景切换、进入关卡、退出关卡和卸载时各模块的行为，避免常驻对象、补丁和资源失控。

3. **以数据驱动功能**  
   KV、OV 和后续功能应尽量建立在稳定的数据模型与运行时接口上，而不是由 UI 直接操作游戏和渲染状态。

4. **性能可测量**  
   不再根据单个 FPS 数字盲目优化。先建立基线、记录帧时间和分配，再针对已定位的热点修改。

5. **配置可演进**  
   从一开始设计清晰的配置版本、校验、默认值和迁移边界。是否兼容 v1 配置需要单独评估，不能默认承诺。

6. **错误隔离与日志节制**  
   单个组件失败不应拖垮整个 Mod；日志应有级别、去重和节流，禁止每帧刷屏。

7. **保持用户体验优势**  
   Fable 最终仍应提供现代、清晰且一致的控制体验，但 UI 技术和最终视觉方案需要在架构阶段确定。

---

## 4. 当前已经确认的原则

- 项目仍然是 ADOFAI 的 UMM Mod。
- 正式产品名称仍是 **CheryTools**，Fable 只是 v2 开发代号。
- 这是从基础开始的重构，而不是 v1 的普通版本更新。
- 用户之后会提供优秀社区项目作为参考案例。
- 在参考项目到齐前，不应急着大量编写功能代码。
- 应先阅读 v1 与参考项目，整理可复用经验和需要规避的问题。
- 所有重要施工前都要建立可回滚备份。
- 不应因为 ADOFAI 3.3.1 的本体性能变化而对 CT 进行没有证据的激进优化。

---

## 5. 尚未确定的事项

以下内容必须在分析和讨论后决定，不要自行假定：

- Fable 的新项目目录、仓库和分支策略；
- UI 最终继续使用 ImGui，还是采用混合方案；
- v2 首个里程碑包含哪些功能；
- KV、OV、Tools 等模块的优先级；
- v1 配置是否迁移，以及迁移到什么程度；
- 支持的最低 ADOFAI 与 UMM 版本；
- 是否引入依赖注入、消息总线或其他框架设施；
- 发布版本号、测试渠道和兼容策略。

遇到这些问题时，应先给出选项、收益和代价，再由项目负责人确认。

---

## 6. 建议的启动流程

### 阶段一：理解

1. 阅读本交接说明；
2. 检查 CheryTools v1 的目录、入口、设置模型和主要模块；
3. 阅读用户随后提供的社区项目；
4. 记录每个项目值得借鉴和不适合照搬的部分。

### 阶段二：设计

1. 绘制 Fable 的模块边界和依赖方向；
2. 定义生命周期和线程边界；
3. 设计配置模型与版本策略；
4. 设计游戏数据快照、运行时处理和渲染提交的流程；
5. 制定最小可运行里程碑与验收标准；
6. 与用户确认后再创建工程。

### 阶段三：施工

1. 创建独立、干净的 Fable 工程；
2. 先完成加载、卸载、日志、配置和空 UI 壳；
3. 建立性能基线和诊断方式；
4. 按确认后的优先级逐个接入功能；
5. 每个阶段都进行编译、游戏内验证和回归测试。

---

## 7. 对后续开发者/AI 的要求

- 不要看到旧代码就立即开始复制或改写。
- 不要把“重构”理解为改类名、拆文件或重新排版。
- 不要未经确认承诺完整兼容 v1 配置。
- 不要为了理论上的性能收益增加复杂度。
- 不要在 Unity 后台线程中直接访问 Unity API。
- 不要让 UI、配置对象和游戏运行时互相直接控制。
- 先给出事实、依赖关系与方案，再开始重大施工。
- 技术选择必须说明解决了什么问题，以及引入了什么成本。

Fable 的目标不是立刻重现 v1 的全部功能，而是建立一个能够可靠承载 CheryTools 下一阶段发展的基础。
