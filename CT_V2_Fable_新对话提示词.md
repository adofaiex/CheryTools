# CheryTools v2「Fable」新对话启动提示词

请先阅读下面的项目背景，并将其作为本次对话的长期上下文：

我们已经结束 CheryTools v1 的主动开发。v1 是一个用于《A Dance of Fire and Ice》的 Unity Mod Manager Mod，主要包含 ImGui 控制面板、KeyViewer、Overlayer、Tag/Token 节点动画、游戏 UI 与编辑器工具等功能。v1 最终封板版本为 **26.2 Beta**，源码位于 `E:\ADOFAI-MOD\CheryTools-main\CheryTools-main`，安装包为 `E:\ADOFAI-MOD\CheryTools-main\CheryTools-main\CheryTools_Beta_26.2.zip`。

现在我们要开发 **CheryTools v2**，开发代号为 **Fable**。正式 Mod 名称和标识仍然属于 CheryTools，Fable 只是用于区分新一代工程的代号。

这里的“重构”是彻底重构：

- 不是继续修改 v1；
- 不是给旧项目重新分文件或改类名；
- 不是先复制整套旧代码再慢慢修；
- 而是重新设计模块边界、生命周期、数据模型、运行时、渲染、配置与 UI 的关系，并建立一个干净的新工程。

v1 只能作为功能、交互、游戏接口和历史经验参考。可以提取经过验证的算法或逆向结果，但不能无判断地照搬旧架构。

我之后会提供一些优秀的 ADOFAI 社区项目源码作为案例。你需要分别分析：

1. 它们的入口和生命周期；
2. Harmony 补丁与游戏数据采集方式；
3. 配置和持久化方案；
4. UI、运行时与渲染的边界；
5. 性能、资源管理和异常处理方式；
6. 哪些部分适合 Fable，哪些部分不应照搬。

当前不要急着写大量代码。第一阶段应当是：

1. 阅读 `CT_V2_Fable_项目交接说明.md`；
2. 理解 CheryTools v1，但不要修改 v1；
3. 等待并分析我提供的社区项目；
4. 输出 Fable 的需求边界、模块架构、依赖方向、生命周期、线程模型、配置策略、性能基线与开发里程碑；
5. 列出尚需我决定的问题；
6. 得到确认后再创建新工程并施工。

开发时必须遵守：

- 默认使用简体中文沟通；
- 重要改动前建立可回滚备份；
- 不覆盖或重置 CheryTools v1 当前源码；
- 不根据单一用户的 FPS 数字盲目优化；
- Unity API 只能在允许的主线程阶段访问；
- 游戏数据采集、业务状态、渲染提交、UI 编辑和持久化需要明确分层；
- 日志必须分级、去重和节流，禁止每帧刷屏；
- 技术方案需要说明收益、代价和可替代方案；
- 未经确认，不承诺 v1 配置兼容，也不自行决定 Fable 的 UI 技术和首发功能范围。

如果当前只拿到了这份提示词，请先阅读上述 v1 路径中的项目，简要复述你对 Fable 的理解，并等待我提供社区参考项目和下一步指示；不要立刻创建工程或修改代码。
