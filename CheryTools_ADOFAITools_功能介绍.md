# CheryTools ADOFAITools 功能介绍

欢迎使用CheryTools！下面是MOD介绍！

## 一、KeyViewer / KV 按键显示系统

CheryTools 内置了高度可自定义的按键显示系统，可以在屏幕上显示玩家当前使用的按键、按键次数、KPS、总点击数以及键雨效果。

### 核心功能

- 支持创建多个 KV 配置。
- 每个 KV 配置都可以独立启用，因此可以同时显示多套按键显示布局。
- 新建配置时可以选择空白配置，也可以从 16K、12K、10K、8K 预设开始。
- 支持旧版 KV 配置迁移，可从旧 Settings.xml 或旧 .cyt 备份中导入旧的 16K/12K/10K/8K KV 布局。
- 支持仅游戏时显示 KV。
- 支持限制输入，只允许 KV 中配置的按键参与输入，减少误触或多余按键干扰。
- 支持重置统计数据。

### KV 节点类型

- 普通按键：显示单个按键的按下状态和计数。
- KPS 面板：显示当前每秒按键数。
- Total 面板：显示总按键数。
- 背景贴图：可以添加图片作为 KV 的背景或装饰元素。

### FreeMake 编辑器

- 可在游戏内可视化编辑 KV 布局。
- 支持添加、删除、拖动、缩放和调整按键节点。
- 支持多选节点并批量移动。
- 支持节点深度设置，用来控制同一 KV 配置内不同节点的遮挡关系。
- 支持背景贴图节点设置为不可选，避免误拖动。
- 更改按键绑定后，可以自动同步显示为新按键文本。
- 支持隐藏单个按键的计数数字，隐藏后按键文字会居中显示。

### KV 外观

- 每个 KV 配置都有独立的外观设置，调整一个配置不会影响其他配置。
- 支持整体缩放、默认按键大小、边框粗细。
- 支持自定义字体路径。
- 支持背景色、边框色、文本色、KPS 颜色、Total 颜色。
- 支持普通状态和按下状态分别设置颜色。
- 支持按键文字描边。
- 支持计数文字描边。
- 支持按键文本、计数文本的偏移和缩放。
- 支持节点级自定义颜色、字体、文字、计数显示、描边和深度。

### 键雨功能

- 按键按下时可以生成向上飞行的键雨。
- 支持键雨速度、消失距离、颜色、宽度比例和高度偏移设置。
- 支持两排键雨分别设置。
- 支持高度裁剪和羽化透明两种消失模式。
- 支持单个按键独立设置键雨颜色、宽度比例和高度偏移。

### KV 配置包

- 支持单独导出 KV 配置为 .ctkv。
- 支持从 .ctkv 导入 KV 配置。
- 导出的 .ctkv 可包含 KV 使用到的外部图片和字体资源。


## 二、Overlayer / OV 屏幕叠加系统

Overlayer 是 CheryTools 的屏幕信息叠加系统，可以在游戏画面上自由添加文本、图片和进度条，适合制作直播/录制用信息面板、状态栏、装饰 UI 或自定义统计显示。

### 通用功能

- 支持开启/关闭整个 OV 系统。
- 支持仅游戏时显示 OV。
- 支持编辑模式下解锁拖动。
- 支持模块快速对齐到屏幕九宫格位置。
- 支持 Pivot 锚点设置，让不同对齐方式下的位置更稳定。
- 支持深度设置，范围为 -100 到 +100，用来控制 OV 模块之间的遮挡关系。
- 支持覆盖层刷新率设置。
- 支持图片渲染倍率设置，用于在清晰度和性能之间取舍。
- 支持导出/导入 OV 配置包。

### OV 文本模块

- 可以创建多个文本模块。
- 每个文本模块都可以独立启用、命名、设置位置和深度。
- 支持自定义文本公式，通过 Tag 显示游戏数据。
- 支持富文本颜色和字号标签。
- 支持 Tag 快捷插入窗口，按分类插入常用 Tag。
- 支持效果预览，便于在设置窗口内查看格式效果。
- 支持默认颜色、字号、对齐方式、行高偏移、字距设置。
- 支持自定义字体路径。
- 支持文字阴影，包含阴影颜色、偏移和柔化设置。
- 支持文字描边，包含描边颜色和描边宽度。
- 支持动画系统，可对文本做缩放、移动、旋转、透明度变化等效果。

### OV 图片模块

- 可以创建多个图片模块。
- 支持自定义图片路径。
- 支持缩放、旋转、不透明度、位置、锚点和深度。
- 支持快速对齐。
- 支持图片动画。
- 外部图片资源可以随 .ctov 或 .cyt 包一起导出。

### OV 进度条模块

- 可以创建多个进度条模块。
- 支持自定义进度条名称和启用状态。
- 支持选择进度条数值来源。
- 支持设置 0% 数值来源和 100% 数值来源。
- 支持常量、地图进度、准度、X 准度、KPS、当前真实 CPS、地图时间、音乐时间、Combo、死亡数、错过数、按太快数等数据源。
- 支持从左到右、从右到左、从下到上、从上到下四种填充方向。
- 支持反向填充。
- 支持限制数值到 0%-100%。
- 支持位置、宽度、高度、不透明度、锚点和深度设置。
- 支持背景颜色、填充颜色、边框颜色。
- 支持边框粗细。
- 支持圆角设置。
- 支持进度条阴影，包含阴影颜色、偏移和柔化设置。

### OV 配置包

- 支持单独导出 OV 配置为 .ctov。
- 支持从 .ctov 导入 OV 配置。
- 导出的 .ctov 可包含 OV 使用到的外部图片和字体资源。


## 三、OV 可用 Tag

### 游戏数据

- {fps}：当前 FPS。
- {fps:1}：当前 FPS，保留 1 位小数。数字可以替换为其他小数位数。
- {kps}：当前每秒按键数。
- {tot}：总按键次数。
- {combo}：当前 Pure Combo。
- {combo:p}：当前 Perfect Combo。
- {music}：当前曲目标题。
- {level}：关卡制作者。
- {x}：关卡倍速。
- {ttile} 总轨道数量
- {atile} 经过的轨道数量

### BPM / 点击频率

- {bpm}：基础 BPM。
- {tbpm}：包含轨道速度乘数的 BPM。
- {cbpm}：基于地板时间计算的当前真实 BPM。
- {cur}：当前真实 BPM 下的每秒点击次数。

### 时间和日期

- {maptime}：地图总时间。
- {maptime:p}：地图已游玩时间。
- {musictime}：音乐总时间。
- {musictime:p}：音乐已播放时间。
- {datey}：当前年份。
- {datem}：当前月份。
- {dated}：当前日期。
- {wtime}：电脑时间，24 小时制，精确到时分秒。
- {wtime12}：电脑时间，12 小时制，精确到时分秒。

### 判定和成绩

- {judge}：当前判定模式。
- {interval}：定时窗口大小百分比。
- {acc}：Acc，默认保留 2 位小数。
- {acc:2}：Acc，保留指定小数位数。
- {xacc}：X-Accuracy，默认保留 2 位小数。
- {xacc:2}：X-Accuracy，保留指定小数位数。
- {progress}：地图进度，默认保留 2 位小数。
- {progress:2}：地图进度，保留指定小数位数。

### 判定数量

- {te}：Too Early 数量。
- {ve}：Very Early 数量。
- {ep}：Early Perfect 数量。
- {p}：Pure Perfect 数量。
- {lp}：Late Perfect 数量。
- {vl}：Very Late 数量。
- {tl}：Too Late 数量。
- {fm}：错过数量。
- {fo}：按太快数量。
- {miss}：错过 + 按太快合计。

### 样式标签

- <color=#D4D4D6FF></color>：设置文字颜色，支持透明度。
- <color=#FFFFFFFF></color>：白色文字，带透明度。
- <size=150%></size>：相对字号。
- <size=32></size>：绝对字号。

### 示例

Progress | <color=#D4D4D6FF>{progress} %</color>
ACC | <color=#D4D4D6FF>{acc} %</color>
XACC | <color=#D4D4D6FF>{xacc} %</color>
FPS | <color=#D4D4D6FF>{fps}</color>
Music | <color=#D4D4D6FF>{music}</color>


## 四、Tools - 优化功能

### CheryTools 提供了一些面向打谱和测试场景的优化选项

- 老版本 Pause 修复：用于修复 ADOFAI 官方更新后部分旧谱 Pause 计拍方式变化导致的节奏错位问题。
- 禁用自动播放空格暂停：在编辑器自动播放时，阻止空格键暂停自动播放。
- 禁用播放时滚轮缩放：在播放过程中阻止鼠标滚轮缩放，减少误操作。


## 五、Tools - 视觉功能

视觉功能主要用于调整游戏内实际游玩画面的显示效果。

- 支持隐藏指定类型的判定文本
- 支持自定义星球颜色和拖尾颜色


## 六、Tools - 游戏 UI 控制

CheryTools 可以接管部分游玩界面的原版 UI，用来控制其可见性、透明度、缩放和位置。

### 可控制的 UI

- 关卡名称。
- 倒计时。
- 准度条。
- 判定模式切换。
- 不会失败模式开关。
- 自动演奏开关。
- 自动播放文字。
- 测试版本水印。

### 说明

- 游戏 UI 控制只针对关卡游玩界面生效，不接管菜单、选关或普通关卡编辑器 UI。
- 部分敏感 UI 默认只允许隐藏，位置、缩放和透明度等高级控制需要在开发者选项中输入 Key 解锁。
- 可以一键重置全部 UI 控制，也可以单独重置某一项。


## 七、配置、资源与分享

### CheryTools 对配置分享做了专门支持

- 支持导出完整配置为 .cyt。
- 支持导入完整 .cyt 配置。
- 支持导入旧版 XML 配置。
- 支持 KV 单独导出/导入 .ctkv。
- 支持 OV 单独导出/导入 .ctov。
- 当配置使用外置字体或图片时，资源会统一整理到 CheryToolsAssets 文件夹。
- 导出的 .cyt、.ctkv、.ctov 可以携带相关 Assets，方便分享给其他玩家。


适合的使用场景

- 直播或录制时显示按键、KPS、总按键数和键雨。
- 制作自定义信息面板，例如 FPS、准度、X 准度、地图进度、当前音乐、BPM、判定数量等。
- 制作个性化 OV 布局，例如文字、图片、进度条组合。
- 为不同玩法或不同视频风格准备多套 KV 配置。
- 隐藏或调整原版 UI，让画面更干净。
- 修复旧谱在新版本中的 Pause 兼容问题。
- 和其他玩家分享自己的 KV、OV 或完整 CheryTools 配置。


其他
如果碰到了BUG，请使用站内私信或加群 239996243 得到回复和解决方案

最后，祝你早安，午安，晚安！
